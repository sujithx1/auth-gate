{
  "name": "authgate-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/adapters/*",
    "apps/*"
  ],
  "scripts": {
    "dev:server": "bun --watch apps/auth-server/src/index.ts",
    "dev:dashboard": "bun --cwd apps/admin-dashboard dev",
    "dev": "bun run dev:server & bun run dev:dashboard",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bun-types": "^1.3.14",
    "typescript": "^5.3.3"
  }
}
